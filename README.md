# data_pipeline
We will build an end-to-end data pipeline and use Git and GitHub to manage the complete development lifecycle.  The pipeline will extract data from a source system, perform data validation and transformations, and load the processed data into a target data warehouse or database.
